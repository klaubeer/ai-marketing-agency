# Copie para .env.prod e preencha antes do primeiro `docker compose up`
# ATENÇÃO: senhas só com letras e números (sem @, :, /, ?, # — quebram parser de URL)

OPENAI_API_KEY=sk-...

# Sentinela — observabilidade dos agentes
SENTINELA_API_KEY=sk-...
SENTINELA_URL=https://api.sentinela.klauberfischer.online
